[project]
name = "edulia-api"
version = "0.1.0"
description = "Edulia API — multi-tenant education platform"
requires-python = ">=3.11"

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
